module.exports = {
    // replace : function(replaceArray) {
    //     return replaceArray.map(replaceArray => replaceArray.replace(/'/g, '"'));
    // },
    replace : function(replaceArray) {
        return replaceArray.replace(/'/g, '"');
    },
}